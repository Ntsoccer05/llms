# 今回のアプリで直接使用しているパッケージのみ（他は依存で入る）
# FastAPI
fastapi==0.135.1
uvicorn==0.41.0
python-multipart==0.0.22

# Streamlit
streamlit==1.55.0

# 設定・バリデーション（.env 読み込み用）
pydantic==2.12.5
pydantic-settings==2.13.1
python-dotenv==1.2.2

# HTTP クライアント
httpx==0.28.1

# AWS（Bedrock / Strands）
boto3==1.42.61
strands-agents==1.29.0
